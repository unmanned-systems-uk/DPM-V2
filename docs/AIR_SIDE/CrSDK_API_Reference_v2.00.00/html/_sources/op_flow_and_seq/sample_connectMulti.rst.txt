.. _sample-connectMulti:

sample code - "connect to multi-camera"
=======================================

.. code-block:: cpp

    usage:
       set <camera index> <DP name> <param> [1-blocking,0-no blocking]
       get <camera index> <DP name>
    To exit, please enter 'q'.

Save the following code as "RemoteCli.cpp", replace "app/RemoteCli.cpp" in the SDK, then build.

.. literalinclude:: /_static/connectMulti.cpp
   :language: cpp
   :linenos:
