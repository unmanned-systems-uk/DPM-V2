- always use carriage return to submit tmux messages
- CRITICAL: protocol/*.json files are SINGLE SOURCE OF TRUTH for ALL cross-domain standards. ALL domains MUST enforce protocol compliance at runtime. NEVER allow hardcoded values that should be in protocol files.
- CRITICAL: protocol/*.json files are SINGLE SOURCE OF TRUTH for ALL cross-domain standards. ALL domains MUST enforce protocol compliance at runtime. NEVER allow hardcoded values that should be in protocol files.
- CRITICAL: protocol/*.json files are SINGLE SOURCE OF TRUTH for ALL cross-domain standards. ALL domains MUST enforce protocol compliance at runtime. NEVER allow hardcoded values that should be in protocol files.
- ALWAYS update protocol JSON files BEFORE implementing network commands. protocol/commands.json is the single source of truth. If Air-Side and SystemTools disagree, check the protocol first.
- ALWAYS update protocol JSON files BEFORE implementing network commands. protocol/commands.json is the single source of truth. If Air-Side and SystemTools disagree, check the protocol first.
- ALWAYS update protocol JSON files BEFORE implementing network commands. protocol/commands.json is the single source of truth. If Air-Side and SystemTools disagree, check the protocol first.
- ALWAYS use protocol/log_contexts.json for log contexts. ALL domains MUST enforce context parameter at runtime (Air-Side: LogContext enum, SystemTools: ProtocolLogger wrapper, Ground-Side: StructuredLogger). NEVER allow logs without context tags.
- ALWAYS use protocol/log_contexts.json for log contexts. ALL domains MUST enforce context parameter at runtime (Air-Side: LogContext enum, SystemTools: ProtocolLogger wrapper, Ground-Side: StructuredLogger). NEVER allow logs without context tags.
- ALWAYS use protocol/log_contexts.json for log contexts. ALL domains MUST enforce context parameter at runtime (Air-Side: LogContext enum, SystemTools: ProtocolLogger wrapper, Ground-Side: StructuredLogger). NEVER allow logs without context tags.
- NEVER close GitHub issues before verifying git commits exist. Run git log --oneline --grep='#XXX' to confirm code is committed. Record commit hash in issue comment before closing.
- NEVER close GitHub issues before verifying git commits exist. Run git log --oneline --grep='#XXX' to confirm code is committed. Record commit hash in issue comment before closing.
- NEVER close GitHub issues before verifying git commits exist. Run git log --oneline --grep='#XXX' to confirm code is committed. Record commit hash in issue comment before closing.
- Commit .claude/commands/, .claude/PM_*.md, and .claude/*_CRITICAL.md to git. DO NOT commit .claude/settings.local.json (user-specific).
- Commit .claude/commands/, .claude/PM_*.md, and .claude/*_CRITICAL.md to git. DO NOT commit .claude/settings.local.json (user-specific).
- current SystemTools is DPM_Management_System.py NOT main.py
- any updates to DPM_Management_System.py must increment Version ID version.json